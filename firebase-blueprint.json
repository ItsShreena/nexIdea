{
  "entities": {
    "user": {
      "title": "User Profile",
      "description": "User account registration and public references.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "Registration email address"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of profile creation"
        }
      },
      "required": ["email", "createdAt"]
    },
    "startup_analysis": {
      "title": "Startup Analysis",
      "description": "Venture analysis results generated by NexIdea.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "UID of the user who owns this assessment, or 'anonymous'"
        },
        "name": {
          "type": "string",
          "description": "The name of the validated venture"
        },
        "ideaDescription": {
          "type": "string",
          "description": "The original concept text entered by the user"
        },
        "overallScore": {
          "type": "number",
          "description": "Computed overall scalability and science viability score out of 10"
        },
        "result": {
          "type": "object",
          "description": "Full serialized AnalysisResult object containing SWOT, MVP features, timeline, monetization, and finances"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp"
        }
      },
      "required": ["userId", "name", "ideaDescription", "overallScore", "result", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "user",
      "description": "Path to individual user profile records."
    },
    "/startup_analyses/{analysisId}": {
      "schema": "startup_analysis",
      "description": "Path to all validated venture assessment reports."
    }
  }
}
